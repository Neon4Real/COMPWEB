const WA='971559956683';
document.querySelectorAll('.concierge-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);const lines=['Hello Live World,','',form.classList.contains('quote-form')?'Mobile phone quotation request':'Service / repair booking',...Array.from(d.entries()).map(([k,v])=>`${k}: ${v}`)];window.open(`https://wa.me/${WA}?text=${encodeURIComponent(lines.join('\n'))}`,'_blank','noopener');}));
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth'});}}));
const hero=document.querySelector('.hero');hero?.addEventListener('pointermove',e=>{const b=hero.getBoundingClientRect();hero.style.setProperty('--pointer-x',`${e.clientX-b.left}px`);hero.style.setProperty('--pointer-y',`${e.clientY-b.top}px`);});
const reveal=document.querySelectorAll('.product-story,.repair-section,.concierge-section,.reviews-section,.visit-section');reveal.forEach(el=>el.classList.add('reveal-ready'));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('revealed');observer.unobserve(entry.target);}}),{threshold:.12});reveal.forEach(el=>observer.observe(el));
